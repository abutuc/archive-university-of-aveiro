### Integration Test

When running the command  ```mvn install failsafe:integration-test``` there are run 4 tests, 2 from the geocoding folder and the rest from the integration folder.

The tests from the integration folder take slightly longer to run and fail if there is no internet connection.

